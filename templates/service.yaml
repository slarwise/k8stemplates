apiVersion: v1
kind: Service
metadata:
  name: $NAME
  labels:
    app: $POD_SELECTOR
spec:
  ports:
    - name: $PORT_NAME
      port: $PORT
      targetPort: $TARGET_PORT
  selector:
    app: $POD_SELECTOR
